# Activity Log

## 2026-08-13 — Phase 6: Field Survey module (complete)

Built all five sub-modules of the Field Survey module, mirroring the Housing Management scaffolding recipe.

- **6.1 Survey Forms** (`field_survey_forms`): survey type/template definitions (form code, title, category, subject type). CRUD page, API, JS bridge. New production module + resource created: "Field Survey Management", gated the same way as every other module.
- **6.2 Survey Assignments** (`field_survey_assignments`): assigns a form to a surveyor for a Resident, Household, or Site (polymorphic `subject_type`/`subject_id`, or free-text site label). Resident/Household pickers reuse the existing `residents.php`/`households.php` search endpoints.
- **6.3 Survey Results** (`field_survey_results`): one result per assignment (unique FK). Recording a result auto-marks its assignment `Completed` — a cross-module business rule mirroring Housing's beneficiary-award-sets-unit-occupancy pattern.
- **6.4 Photo Uploads** (`field_survey_photos`): image evidence attached to a result, managed inline from the Result's view modal (not a standalone page), same upload pattern as `resident_documents` (`uploads/field-surveys/`, random stored filenames, 5MB limit, image MIME whitelist).
- **6.5 Survey History**: no new table — a computed, date-sorted timeline merging assignments + results for a given subject (`FieldSurveyAssignmentRepository::getHistoryForSubject()`), same approach as `HousingUnitRepository::getHistory()`. Standalone lookup page since Site subjects have no existing profile page to attach the timeline to.

All sub-modules use the same `field survey management` permission gate — no additional production resources needed beyond the one created for Survey Forms. Verified via standalone PHP CLI test scripts (repository/service layer) plus live browser testing of every CRUD and cross-module flow.

## 2026-08-13 — Phase 7: Program Analytics (complete)

Built a new read-only analytics page — **not** a modification of the existing `pages/dashboard.php`, which is owned by the Scrum Master and uses hardcoded/mock citywide data unrelated to this capstone's modules.

- `src/Repositories/AnalyticsRepository.php`: cross-module aggregation queries (no new tables) — `summary()`, `housingOccupancyChart()`, `urbanProjectStatusChart()`, `surveyConditionChart()`, `kpis()` (housing occupancy / survey completion / urban project completion rates), `recentActivity()` (UNION ALL feed across residents, housing_units, urban_projects, field_survey_assignments).
- `api/employee/analytics.php`: single combined GET endpoint, gated by a new `program analytics` resource.
- `pages/analytics/overview.php` + `assets/js/analytics/analytics.js`: follows the codebase's existing precedent for chart-heavy pages (`dashboard.php`/`dashboard-analytics.js`) — Chart.js loaded via direct `<script>` tag, not the CRUD bridge system, since this page has no create/edit/delete flows. Reuses the existing `assets/css/dashboard-analytics.css` glass-panel styling for visual consistency with the rest of the portal.
- New "Program Analytics" module + resource created in production Module Management / Resource Management (same manual step as every other module); sidebar entry added under a new "Analytics" section.

Verified via a standalone PHP CLI test (25 assertions covering all repository methods, including a manual cross-check of the housing occupancy rate KPI math) plus live browser testing — all three charts, four summary cards, six-item recent-activity feed, and three KPI bars render correctly against real seed data from the prior phases.

## 2026-08-13 — Phase 10 (start): Gemini API backend connectivity verified

Skipped ahead to Phase 10 at user request (Phases 8-9 still pending). This step is backend-only per user instruction — no AI Planning Assistant UI yet.

- `GEMINI_API_KEY` added to `.env` (already gitignored; never referenced in source, never sent to frontend).
- `src/Services/GeminiService.php`: reusable service wrapping the Gemini `generateContent` REST API via cURL, matching the existing `config/proxy.php` cURL conventions rather than pulling in a new HTTP client library.
- `api/employee/gemini-test.php`: minimal auth-gated test endpoint (`?prompt=...`), no permission resource yet since this isn't a real module.
- **Model name note**: `gemini-2.0-flash` and `gemini-2.5-flash` both returned 404 "no longer available" for this key despite being listed in `ListModels` — settled on the `gemini-flash-latest` alias, which works. Worth rechecking if Google changes model availability again.
- Verified with a CLI script (real API round-trip, not mocked) and by hitting the endpoint in an authenticated browser session — both returned a real Gemini response.

## 2026-08-13 — Phase 10 (continued): AI Planning Assistant UI

Built the full chat interface on top of the verified Gemini backend, covering the remaining Phase 10 bullets (AI Planning Recommendations, Report Summaries, Natural Language Queries, AI Chat Interface) as one cohesive feature rather than four separate UIs — quick-action buttons just send tailored prompts through the same grounded chat endpoint.

- `GeminiService::chat()`: multi-turn conversation support (role-tagged history) plus `buildSystemInstruction()`, which pulls a live snapshot from `AnalyticsRepository` (totals, KPIs, chart breakdowns) into the Gemini `systemInstruction` field so answers are grounded in real numbers instead of invented ones. `AnalyticsRepository` injected into `GeminiService` via `bootstrap.php`.
- `api/employee/ai-assistant.php`: POST endpoint accepting `{message, history}`, capped at the last 20 turns to keep payload/prompt size reasonable. Gated by a new `ai planning assistant` resource (same manual production Module Management / Resource Management step as every other module).
- `pages/ai-assistant/chat.php` + `assets/js/ai-assistant/{app,api,chat,events}.js`: chat bubble UI (user right-aligned, assistant left-aligned), three quick-action buttons (Summarize Program Status, Generate Planning Recommendations, Explain Housing Occupancy), lightweight markdown-lite formatter for Gemini's `**bold**`/bullet output (no markdown library added, per project convention). Conversation state is client-side only for now — no chat-history table.
- Sidebar: new "AI Assistant" section with a single "Chat" link.

Verified with a CLI test (grounded question answered with the exact real housing-unit count and occupancy rate; a multi-turn follow-up correctly derived the vacant-unit count from prior context) and live in the browser: asked "how many residents and households" (got the exact real numbers) and used the "Generate Planning Recommendations" quick action, which produced four specific, correctly-grounded recommendations referencing the actual Poor-condition survey result, the one Planned-but-not-started urban project, and the 50% housing occupancy rate.

## 2026-08-14 — Phase 7 revisited: `pages/dashboard.php` converted to live capstone data

Reverses the 2026-08-13 Phase 7 decision to leave `pages/dashboard.php` untouched (it was previously hardcoded/mock citywide data owned by the Scrum Master). At explicit user request, the landing dashboard itself now reflects this capstone's real data instead of routing everyone to the separate `pages/analytics/overview.php` page.

- `pages/dashboard.php`: replaced all hardcoded widgets (fake citizen counts, mock KYC rate, a scripted "Maria Santos" activity feed) with the same real-data markup pattern as `pages/analytics/overview.php` — 4 summary cards (Residents/Households, Housing Units, Urban Projects, Survey Results/Assignments), a records-overview bar chart, housing-occupancy and project-status doughnuts, a real recent-activity feed, and 3 KPI bars.
- `assets/js/dashboard-analytics.js`: rewritten to fetch `api/employee/analytics.php` (already built for the Analytics module, reused as-is — no new endpoint) and render the charts/feed/KPIs from live `AnalyticsRepository` output instead of static numbers.
- `includes/sidebar.php`: added a top-level "Dashboard" link (not gated behind any resource permission, since it's the universal post-login landing page like Profile — every authenticated user can reach it, same as before the hardcoded version existed but now with an actual nav entry instead of only being reachable by direct URL after login).
- `includes/footer.php` / `pages/dashboard.php`: added `filemtime()`-based cache-busting query strings to the `dashboard.js` and `dashboard-analytics.js` `<script>` tags, which previously had none — editing either file could otherwise serve a stale cached copy, the same class of bug `ASSET_VERSION` exists to prevent for bridge-loaded JS.

Verified live in-browser (superadmin session): sidebar "Dashboard" entry highlights correctly, skeleton-to-real-content transition works, and every widget matches actual seed data (2 residents, 1 household, 2 housing units, 1 urban project at 0% completion, 1 survey result at 100% completion, 50% housing occupancy, real activity feed rows like "Barangay 176 Road Widening" and "HU-TEST-02").

## 2026-08-14 — Phase 8: Reports module (complete)

Built the Reports module — read-only, filterable, exportable, and printable listings over the four record-holding modules (Residents, Housing, Urban Projects, Field Survey). No new tables: every repository involved already had `paginate($filters, $page, $perPage)` and `stats()`, so Reports reuses those rather than duplicating query logic.

- `src/Repositories/{Resident,HousingUnit,UrbanProject,FieldSurveyResult}Repository.php`: added optional `date_from`/`date_to` filters to each `paginate()` (backward compatible — only applies when the filter is passed; existing CRUD pages are unaffected). Field Survey filters on `survey_date`; the other three filter on `created_at`.
- `api/employee/reports.php`: single endpoint dispatching on `?type=resident|housing|project|survey`. `GET` returns `{stats, listing}` JSON; `GET ...&export=csv` streams a CSV of every matching row (bypasses pagination) via `fputcsv`, gated by a new `reports` resource.
- `pages/reports/{resident,housing,project,survey}-reports.php` + `assets/js/reports/reports.js`: one shared, config-driven script (per-type filter fields, stat-card key mapping, row renderer) rather than four near-duplicate files — loaded directly per page like `dashboard-analytics.js`/`analytics.js` (no CRUD bridge needed, this is read-only).
- **PDF export decision**: no PDF library added (project has none, and CLAUDE.md prefers not introducing new dependencies). Instead, `assets/css/reports-print.css` (`@media print`, hides sidebar/header/filter bar/buttons) plus a "Print / Save PDF" button that calls `window.print()` — the browser's native Save-as-PDF covers both the "Export to PDF" and "Printable Reports" bullets with the same mechanism. User confirmed this approach explicitly before implementation.
- Sidebar: new "Reports" section with the four links, gated the usual way (`$hasResourceAccess(['reports', 'report management'])`). New "Reports" module + resource created in production Module Management / Resource Management, granted to the Urban Planning Administrator role in Permission Builder.
- Also fixed, while investigating why the new Dashboard sidebar link and this section weren't rendering: `includes/footer.php` and the direct-loaded `dashboard-analytics.js`/`reports.js` `<script>` tags had no cache-busting query string at all (unlike the bridge system's `ASSET_VERSION`) — added `filemtime()`-based versioning to each, same class of fix as the existing `ASSET_VERSION` mechanism.

**Observed but not touched**: opening Permission Builder for the "Planning Officer" role and searching "Reports" showed the resource pre-rendered with Master Access and every action checkbox already checked, despite never being explicitly saved for that role. `state.js`/`ui.js` render this purely from `currentPermissions[roleId]`, which is itself built only from the remote GET response's real `role_permissions` rows (the client-side auto-grant block was already removed per the 2026-08-14 Permission Builder bug fix below) — so if this wasn't already true before this session, the remote production backend itself appears to have applied the "Urban Planning Administrator" save to other roles too. Did not click Save for Planning Officer, so nothing was persisted by this session either way. Worth the Scrum Master checking role_permissions for Planning Officer in production directly.

Verified live in-browser (Urban Planning Administrator session): all four report pages render real seed data (2 residents, 2 housing units, 1 urban project, 1 survey result), a housing occupancy filter correctly narrows the table while stat cards stay at overall totals, and CSV export confirmed via Apache's access log (`200`, correct byte counts for both the unfiltered 2-row and filtered 1-row exports).

## 2026-08-14 — New: Beneficiary Application Documents (citizen self-service upload, staff review/approve)

User asked where citizens/applicants upload documents for admin review. Audit found: `resident_documents` is staff-uploaded-on-behalf-of only (no status field, no review workflow); Housing Beneficiaries had an `Applicant` lifecycle but zero document support; `pages/citizen/*` is staff tooling for citizen *account* security, unrelated to documents; `api/citizen/*` is a complete proxied citizen auth API (register/login/OTP/profile) implying citizens already have a portal identity on production, but no local document upload existed anywhere. User confirmed: attach documents to the Housing Beneficiary application (not Resident), and that citizen self-upload will arrive via **a future mobile app built later** — a citizen-facing app, distinct from the field-surveyor mobile app already scoped in `docs/mobile-app-plan.md`. This session's scope was therefore: build the full schema + review/approve workflow now, with staff able to upload on an applicant's behalf in the meantime so the review flow is fully usable today, per the same "defer until that client actually starts" precedent already used for the mobile field-survey assignment gap.

- `database/phase12_beneficiary_documents.sql`: new `housing_beneficiary_documents` table — `document_type` (Valid ID / Proof of Income / Barangay Certificate / Certificate of Indigency / Proof of Residency / Other), `submitted_by` (Citizen/Staff, so the future mobile submissions are visually distinguishable from staff-entered ones), `review_status` (Pending/Verified/Rejected), `review_notes`, `reviewed_by_name`, `reviewed_at`.
- `src/Repositories/BeneficiaryDocumentRepository.php` + registered in `bootstrap.php`.
- `api/employee/beneficiary-documents.php`: GET (list for a beneficiary), POST (multipart upload — same validation as `resident-documents.php`: 5MB limit, PDF/JPG/PNG only), PATCH (review: `Verified` or `Rejected`, requires a reason when rejecting, records `$headerUser['full_name']` as reviewer), DELETE. Gated by the existing `housing management` resource (no new resource needed — it's a sub-feature of Beneficiaries). `api/employee/housing-beneficiaries.php`'s single-record GET now attaches `documents` the same way `residents.php` already does.
- `pages/housing/beneficiaries.php`: new "Application Documents" section in the View Beneficiary modal — status badges, submitted-by tag, reviewer/date line, rejection reason, Approve/Reject/Delete actions, upload form. Approve/Reject buttons gated by a new `window.benCanReviewDocuments` flag (superadmin or `APPROVE`/`REJECT` granted actions) — this resource wasn't in `ui.js`'s `resourceSupportedActionsMap`, so those two actions were already grantable via Permission Builder with no changes needed there.
- `assets/js/housing-beneficiaries/documents.js` (new bridge file, registered in `app.js`): render + upload + approve + reject + delete. Bumped `ASSET_VERSION` to `2026-08-14-2`.

**Deferred, matching the mobile plan's own established precedent**: no citizen-facing submission endpoint was built. When the citizen mobile app work starts, it calls this same `POST api/employee/beneficiary-documents.php` (or a citizen-auth-appropriate equivalent) with `submitted_by=Citizen`; the review UI already renders that distinction.

Verified live in-browser end to end: created a test beneficiary (Maria Santos Dela Cruz), uploaded two documents, rejected one with a reason (badge, reason text, and reviewer/date all rendered correctly), approved the other (Verified badge), confirmed both files landed in `uploads/beneficiary-documents/` with randomized filenames.

## 2026-08-14 — Docs: citizen document upload mobile API reference; removed Analytics from sidebar

- `docs/citizen-document-upload-mobile-api.md`: new reference doc for whoever builds the future citizen mobile app's document upload feature. Documents the existing schema/repo/endpoint contract from the Beneficiary Application Documents work above, and explicitly calls out the unresolved gap: `api/employee/beneficiary-documents.php` is staff-session-gated and cannot be called by a citizen client as-is — a new citizen-auth-gated endpoint plus a citizen-account-to-resident matching strategy (likely by email) must be built first. Linked from `docs/mobile-app-plan.md` with a note that there are now two separate future mobile apps (field-surveyor vs. citizen), not one.
- `includes/sidebar.php` / `assets/js/dashboard.js`: removed the "Analytics" sidebar section per user request, since `pages/dashboard.php` now covers the same content (Phase 7 rework earlier today). `pages/analytics/overview.php` and its API/JS were left in place, just unlinked from navigation — not deleted.

## 2026-08-15 — Phase 9: Local Activity Log (complete)

Investigated the existing Audit Trail (`pages/audit/*`) before building anything: it's a **remote-proxy** module (`api/employee/audit-logs.php`, `api/employee/notifications.php` both forward to `civentral.tech`), built by a teammate and merged in from the `civentral-education` branch. It has zero visibility into this capstone's own local-DB modules (Residents, Housing, Urban Planning, Field Survey) since those never call the remote API. User confirmed: build a separate local activity log rather than trying to feed the remote system or skip the gap.

- `database/phase13_activity_logs.sql`: new `activity_logs` table (`user_id`, `actor_name`, `module`, `action` [Create/Update/Archive/Restore/Delete/Approve/Reject], `target_table`, `target_id`, `target_label`, `description`, `created_at`). No FK on `user_id` — local `users` rows only sync in on login.
- `ActivityLogRepository` (create/paginate/stats/distinctModules) + `ActivityLogService::record()` (resolves actor from `$headerUser`, wrapped in try/catch so a logging failure never breaks the write it's attached to), registered in `bootstrap.php`.
- Instrumented the create/update/archive/delete paths of all 15 local-DB write endpoints (Residents, Households, Resident Documents, Housing Units, Housing Beneficiaries, Housing Occupancy, Housing Relocations, Beneficiary Documents, Development Plans, Urban Projects, Infrastructure Records, Planning Documents, Field Survey Forms/Assignments/Results) with a one-line `$activityLogService->record(...)` call each, using each entity's natural code/name as the label and falling back to `"{Entity} #{id}"` when none exists.
- `api/employee/activity-logs.php`: GET-only, filterable by module/action/user/date/search, `?action=stats`. New `activity logs` resource.
- `pages/logs/activity.php` + `assets/js/logs/activity-log.js`: read-only filterable table following the Reports/Analytics precedent (direct-loaded JS, no CRUD bridge). Sidebar: single top-level "Activity Log" link (like Dashboard), not a dropdown, since there's only one page.
- New "Activity Logs" module + resource created in production Module Management/Resource Management. Granting to a role hit an "own role" self-service guard when saving against Urban Planning Administrator (the logged-in account's own role) — expected safety behavior, not a bug. Granted successfully to Planning Officer instead to confirm the save path works; the current session already has full access via `is_global_access`.
- **Found and fixed a pre-existing, unrelated bug while first loading the new page**: `bootstrap.php` was instantiating `BeneficiaryService` with 3 constructor arguments while the class required 4 (`$householdRepo` missing), a fatal error blocking every single page in the app since `bootstrap.php` loads everywhere. Root cause looks like an in-progress duplicate-detection change to `BeneficiaryService` (see `docs/thesis-scope-gap-analysis.md`) whose constructor signature was updated without updating the call site. Fixed by passing `$householdRepo` through.

Verified with a standalone PHP CLI test (6 assertions: search/module/action filters, the widened action ENUM accepting Approve/Reject, stats(), distinctModules(), and that `record()` swallows a simulated repo failure without throwing) plus live browser testing: created and archived a real Development Plan through the UI and confirmed both events appeared on the Activity Log page with the correct actor, module, action badge, and record label, and that the module filter correctly narrows the table.

## 2026-08-14 — Bug fix: Permission Builder silently over-granted custom modules to every role

Fixed a privilege-escalation bug reported for the Planning Officer role: revoking access to a module worked, but the role would also end up with access to unrelated modules (including Main Controls) it was never granted.

- Root cause: `fetchPermissionsData()` in `assets/js/rolespermission/permissions/api.js` had a block ("Automatically grant ALL action permissions for newly created/custom modules across roles") that unconditionally injected every action, for every role, into `window.savedPermissions` whenever a module/resource was still flagged `is_custom` — i.e. a module/resource created via Module/Resource Management whose client-generated local ID never reconciled with its real database ID (falls back to `Date.now()` when the create response doesn't echo the real ID back, which then never matches on a later sync). Because `saveChanges()` POSTs the entire `currentPermissions[roleId]` map as a full replace (not a diff), saving *any* unrelated change for a role also persisted this silently-injected full grant to the database.
- Fix: removed the auto-grant block. New/unsynced modules now start with zero granted permissions for every role, same as any DB-synced resource — an admin must explicitly check them per role in the Permission Builder. The separate `is_custom` bypass in `ui.js` (`isModuleAllowedForDepartment`/`isResourceGrantedToCurrentUser`) is unrelated and untouched — it only controls whether the *currently logged-in admin* can see/toggle a module they just created, not what gets granted to other roles.
- Bumped `ASSET_VERSION` in `assets/js/app.js` per convention.
- **Follow-up needed**: this only stops the bug going forward. Any role (e.g. Planning Officer) that had permissions saved while the bug was active may already carry unintended real grants in the database — review and re-save affected roles in the Permission Builder to clear them.

## 2026-08-15 — Barangay Mapping module (web spatial layer, Phase 16)

Built the spatial layer identified in `docs/thesis-scope-gap-analysis.md` (section G) as foundational for Zoning Clearance and Building Review — scoped to **web only** for this pass; mobile mapping and AI-map integration were explicitly deferred (no React Native app exists anywhere in this repo, and the existing `GeminiService` isn't a query/tool-calling architecture yet, matching the doc's own build-order guidance not to build speculative AI ahead of it).

- **Boundary data**: sourced 188 Caloocan barangay polygons (medium-resolution) from `faeldon/philippines-json-maps` (MIT-licensed, PSA/PSGC Dec 2023 boundaries). The user's manuscript figure of 193 barangays does not match the current official PSA count for Caloocan (188) — flagged to the user, proceeded with 188 as authoritative rather than fabricating 5 extra polygons. Slimmed to `assets/geojson/caloocan-barangays.geojson` (~74KB, one request, matches the "no /public dir — assets/ is the static convention" finding).
- `database/phase16_barangay_mapping.sql`: new `barangays` reference table (`psgc_code`, `name`, `district` nullable — the source has no legislative-district field, `centroid_lat`/`centroid_lng` computed via a shoelace-formula polygon centroid, not a plain vertex average), seeded with all 188 rows. Additive `latitude`/`longitude` (nullable) added to `housing_units` — no new `housing_projects` table, since `housing_units` is this codebase's actual "housing project" concept.
- `BarangayRepository`/`BarangayService` (registered in `bootstrap.php`), `api/employee/barangays.php` (list/search, `?id=` detail+stats, `?action=housing-markers`), gated on the existing `urban planning` resource (same pattern Zoning Clearance used, no new Module Management entry required for this pass).
- Per-barangay stats join by **name** against `housing_units.barangay` (free-text, no FK exists) and roll up beneficiary applications through `housing_units.unit_id` — applications not yet assigned a unit aren't attributable to a barangay, a real data-availability limit called out rather than papered over.
- `pages/urban-planning/mapping.php` + `assets/js/mapping/` bridge (Leaflet 1.9.4 via CDN, loaded only on this page rather than globally in `header.php`): boundary layer with hover/click/search, housing-project marker layer, barangay info panel, two data-backed layer toggles (Barangay Boundaries, Housing Projects). Housing Applications and Urban Projects layers were **not** added — neither table has coordinates yet.
- Extended the existing Housing Units form (`pages/housing/housing-units.php` + its JS bridge) with optional Latitude/Longitude fields so staff can actually populate map markers; validated range (-90..90 / -180..180) in `HousingService`.
- Sidebar: added "Barangay Map" inside the existing Urban Planning dropdown (no `dashboard.js` change needed since no new top-level nav section was created).
- Verified via a standalone PHP CLI test (barangay count, search, detail+stats, marker query, lat/lng validation, cleanup — all passed against the real local DB) and live browser testing of the rendered map (188 boundaries drew correctly, including Caloocan's two disconnected areas — the main city and the Camarin/Bagumbong exclave near La Mesa Watershed — hover tooltips and click-to-select styling confirmed working). The authenticated-fetch round trip (click → barangay info panel) could not be fully confirmed in-browser this session — the automation tab's session returned 401 on **every** local-DB API, including pre-existing ones like `housing-units.php`, indicating a stale/expired session in that tab rather than a defect in this module; worth a quick manual click-through to confirm.
